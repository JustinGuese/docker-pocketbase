{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-ingress" (lower .Release.Name) }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: pocketbase
    release: {{ .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  ingressClassName: cloudflare-tunnel
  rules:
    - host: {{ .Values.ingress.host | default (printf "pb-%s.aigentworkforce.com" (lower .Release.Name)) }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}
                port:
                  number: 8080
{{- end }}
